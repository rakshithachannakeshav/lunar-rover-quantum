# Normalise line endings to LF in the repo (Linux / ROS project).
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.npz binary
*.bag binary
*.pdf binary
