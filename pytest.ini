[pytest]
testpaths = tests
markers =
    ros2: requires a ROS2 runtime (rclpy) - not runnable in CI
    gazebo: requires a running Gazebo simulation - not runnable in CI
addopts = -m "not ros2 and not gazebo"
